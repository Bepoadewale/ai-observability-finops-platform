# Privacy

Prompts, completions, source code, credentials and tool payloads are excluded by default. Correlation uses identifiers in traces and business storage, never Prometheus labels. Development content capture should be explicit, short-retention and access-controlled.
