# Validation

Run `make test lint`; for end-to-end validation run `make bootstrap`, a telemetry generator, then query Prometheus and Tempo and capture only real dashboard screenshots. Record environment and results in `PROJECT_STATUS.md`.
