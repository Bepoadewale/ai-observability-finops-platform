# Security

The demo credentials are non-secret local fixtures. Do not use them outside local development. Administrative and financial views require stronger authorization than tenant-scoped usage views. Never add prompts, completions, credentials, tokens or provider bills to fixtures, metrics labels, dashboards or traces.
