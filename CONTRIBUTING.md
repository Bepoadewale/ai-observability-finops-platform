# Contributing

Run `make test lint` before proposing changes. Preserve explicit units, timezone-aware UTC timestamps, `Decimal` money arithmetic, bounded metrics labels, and data classification labels.
